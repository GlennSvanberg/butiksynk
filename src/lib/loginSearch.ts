/** Full search object for `/login` (TanStack kräver alla validerade nycklar). */
export const defaultLoginSearch = {
  redirect: undefined as string | undefined,
  demo: undefined as string | undefined,
}
